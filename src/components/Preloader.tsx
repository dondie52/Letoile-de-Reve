"use client";

/**
 * Preloader removed for PageSpeed — a full-screen solid overlay delayed
 * Speed Index (~720ms) and kept mobile Performance at 98.
 * Kept as a no-op export so any residual imports stay safe.
 */
export function Preloader() {
  return null;
}
