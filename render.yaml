services:
  - type: web
    name: letoile-de-reve
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx --yes serve@14 out -l tcp://0.0.0.0:$PORT
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
